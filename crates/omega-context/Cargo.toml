[package]
name = "omega-context"
version.workspace = true
edition.workspace = true

[features]
default = []
document-backend = ["dep:omega-hpc-document"]

[dependencies]
omega-client = { path = "../omega-client" }
omega-hpc-document = { path = "../../omega-hpc/crates/omega-hpc-document", optional = true }
omega-hpc-memory = { path = "../../omega-hpc/crates/omega-hpc-memory" }
omega-plan = { path = "../omega-plan" }
omega-hpc-paths = { path = "../../omega-hpc/crates/omega-hpc-paths" }
omega-todo = { path = "../omega-todo" }
omega-tools = { path = "../omega-tools" }
omega-workflow = { path = "../omega-workflow" }
anyhow.workspace = true
serde.workspace = true
serde_json.workspace = true
toml.workspace = true